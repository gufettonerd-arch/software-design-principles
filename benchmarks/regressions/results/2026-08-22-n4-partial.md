# Partial N=4 scale-up — 8 principles at N=4, 2 more at N=3, 2026-08-22

Not a full N=4 run across all 20 principles — that's ~160 more agent runs,
deliberately not attempted in one batch (token-consumption discipline, see
`benchmarks/README.md`). Started as a targeted scale-up of the 3
principles that showed the most signal at N=2: DRY, CQS, and Fail Fast
(05-dry.md, 07-cqs.md, 14-fail-fast.md), each brought to full N=4 (seed3
+ seed4). Extended same day to SOLID and Readability (01-solid.md,
09-readability.md), also full N=4, picked as the next two in line, not
for cause. Extended again to Value Object, Tell Don't Ask, and Law of
Demeter (02-value-object.md, 03-tell-dont-ask.md, 04-law-of-demeter.md),
also brought to full N=4 (seed4 added after the initial seed3-only
pass). Extended once more to Strategy and Specific Exceptions
(06-strategy.md, 08-specific-exceptions.md) — **seed3 only so far, N=3
not N=4 for these two**, noted precisely rather than rounded up. Same
methodology throughout: haiku for Case A (recall), sonnet for Case B
(calibration), baseline told explicitly not to consult any skill,
with-skill told to use it.

Verbatim responses for everything below are saved in
[2026-08-22-n4-raw-transcripts.md](2026-08-22-n4-raw-transcripts.md) —
the first time this project has saved raw transcripts to disk instead of
only summarizing them, closing the gap `grade-principles.md` flagged
since the first run (a genuinely blind regrade needs real saved
responses, not paraphrase from a conversation that ends with the
session). That regrade happened same day —
[2026-08-22-blind-grading.md](2026-08-22-blind-grading.md) — and found a
real error in the Fail Fast section below that this report's own
same-session grading missed on the first pass; corrected below rather
than left standing.

## Results

### Fail Fast — fix confirmed stable, mostly — corrected after a blind regrade

Both seed3 and seed4, Case A, both arms: clean. Case B (the one that had
a repeated false-positive at N=2, root-caused and fixed by adding
`@Valid` + an explanatory comment to the case file) looked clean too on
first, same-session read — the underlying fix (a complete validation
boundary) held.

**Correction, found by [a later blind grading
pass](2026-08-22-blind-grading.md)**: same-session grading missed a real
miss. The with-skill seed4 response suggested `@NotEmpty` on `items` as
a cleaner alternative to the manual check — read inline as a minor
consistency suggestion, passed without a second look. An independent
blind grader caught what that missed: the case file's own comment
claimed "at least one item" "isn't expressible" via bean-validation
annotations, and that's factually wrong — `@NotEmpty`/`@Size(min=1)` are
standard Jakarta Bean Validation constraints that work directly on
`List` fields. Two separate with-skill responses (seed3 and seed4)
independently reached for this same real observation — convergent
evidence it was a correct catch, not a fluke. **The case file had a
genuine defect in its premise, fixed same day** (see the blind-grading
report for the exact change) — the "stable" framing below undersold how
close this got to shipping an unverified claim as settled.

One more borderline, already flagged inline: seed4's Fail Fast Case B
baseline called the manual `items().isEmpty()` check "correct, just
slightly redundant" — didn't recommend removing it, but "redundant" is a
hair closer to a miss than a clean pass. The blind grader agreed,
scoring it a hedged match rather than clean. Logged as-is rather than
smoothed over.

**Reverified same day, 4 fresh with-skill runs against the corrected case
file: 4/4 clean.** None suggested `@NotEmpty`, none treated the manual
check as redundant — all four explicitly named the shared-DTO reasoning
as the correct call ("that's Fail Fast applied at the actual trust
boundary," "the comment's reasoning holds... not a violation of DRY,"
"wouldn't push this logic into a `@NotEmpty` on the DTO"). Unlike DRY's
three failed fix attempts earlier today, this one worked on the first
try — the difference being this was a factual correction (the premise
was simply wrong) rather than an attempt to change a judgment call the
model keeps landing on differently. Each of the 4 runs still found
independent, legitimate secondary issues (IDOR risk on `customerId`
ownership, bare-string error body, unvalidated per-item fields) — noted
as healthy signal, not noise: the fix narrowed the case to its intended
point without making the reviews shallower.

### CQS Case A baseline — investigated: real ~50% recall, not a systematic miss

Seed3 and seed4 both missed the actual violation (`hasSufficientFunds`
promising a pure query but writing to an audit log as a side effect) —
instead flagging real but beside-the-point issues: `double` for currency,
missing null checks, a missing constructor. That looked like a new,
repeatable miss, so two more seeds (seed5, seed6) were run to check.
Seed6 caught it cleanly, naming "command-query separation" outright;
seed5 missed it again, same generic-checklist pattern.

**Full picture across 6 seeds**: baseline hit seed1, seed2, seed6 and
missed seed3, seed4, seed5 — **3/6 (50%)**, not the 0/2 the first two new
seeds suggested. With-skill is 4/4 on the seeds it was run against (no
with-skill runs at seed5/seed6, since those two were baseline-only,
launched specifically to size the baseline rate).

**What the misses have in common, read across all 4 losing transcripts**:
every one opens with the same short list of well-known, syntactic Java
review idioms — `double` for currency, missing null guards, missing
input validation, sometimes a missing-constructor compile-error read —
and never gets to "the method name promises a pure check but the body
writes to a log." The 2 hits, by contrast, name the mismatch directly
and immediately ("Flag: Side effect in a query method... violating
command-query separation"). Working hypothesis, not yet proven: CQS
violations require reading method-name-vs-behavior semantics, which
isn't a standard "common Java pitfalls" idiom the way `BigDecimal`-for-
money is — so an unprimed haiku reviewer's checklist reflex crowds it out
roughly half the time. With-skill's explicit, named list of 20 principles
(CQS among them) directly compensates for that gap by making it something
to check for rather than something to notice unprompted. Consistent with
this project's existing thesis (the skill's value is structure/naming,
not raw capability), just showing up on recall variance here instead of
the calibration-precision axis already documented.

Not settled: 3/6 vs. 4/4 with-skill is still a small sample, and the
mechanism above is read off 4 transcripts, not tested by varying the
snippet to isolate the semantic-vs-syntactic distinction. Worth a
dedicated small experiment (e.g. a case where the syntactic smells are
removed, leaving only the CQS mismatch) rather than more seeds on the
same snippet, if this gets picked up again.

### CQS Case B — case file confound, not a skill/model issue

All 4 new runs (both arms, both seeds) never explicitly addressed the
calibration point being tested (does `metrics.increment` inside a query
count as a CQS-breaking side effect). Instead, every single run found and
focused on other real bugs in the snippet: `cache.put(id, loaded)` caching
a null on a miss, a get-then-put race condition, and (in the baseline
prompts, which don't get the surrounding class context) undeclared
`metrics`/`repository` fields reading as a compile error.

This is the same shape of problem the Fail Fast Case B fix addressed
2026-08-19: a case file with enough *other* legitimate things to say that
the one specific point being measured gets silently skipped rather than
explicitly reasoned through. Per the grading rubric's own standard
("silently missing it because it never looked is not the same as
correctly deciding it's fine"), none of these 4 runs cleanly demonstrates
correct calibration — even though none of them incorrectly flagged CQS
either. The case doesn't currently isolate the thing it's meant to test.

**Fixed same day**: declared `repository`/`metrics` as fields (removed
the "undeclared field" read) and added a comment addressing the
null-caching and race-condition tangents as accepted tradeoffs, same
shape as the Fail Fast fix. Verified with one fresh baseline + with-skill
run: both now explicitly reason through the metrics point instead of
being pulled toward the other bugs — baseline: "incrementing on miss
regardless of outcome is correct"; with-skill: "I considered it and it's
correct." One run each, not a full reseed of Case B — worth folding into
the next N=2/N=4 pass rather than treated as fully re-validated yet.

### DRY Case B with-skill — investigated: real ~33% miss rate, and it's calibration, not a case-file problem

Clean on seed1, seed2, seed3. Missed on seed4. Two more seeds (seed5,
seed6) were run to check whether that was noise: seed5 clean, seed6
missed again. **Full picture across 6 seeds: 4/6 (67%) correct**, 2
misses — a real rate, not a single fluke.

Unlike CQS Case B, this case file has nothing wrong with it — no
undeclared fields, no competing bugs, nothing to get distracted by. Both
misses read the exact same two 1-line classes as the 4 hits and reasoned
their way to the wrong conclusion on a genuinely close call. The 2 misses
share a tell: seed4 asserts "an obvious third [variant] on the way" —
a growth signal invented, not present in the snippet — and seed6 concedes
"only two near-identical one-liners" but still calls it "the DRY smell
here" needing a fix (a lighter one than a full interface, but a fix). The
4 hits, by contrast, explicitly invoke Rule of Three / YAGNI and stop.

This is the one principle-precision miss in this whole investigation that
isn't explained by a fixable case-file defect — it's the skill sometimes
over-applying DRY on a case designed to test exactly that failure mode
(principles.md's own stated risk: "the most common mistake, once you know
a principle, isn't ignoring it — it's overusing it"). Nothing to fix in
the case file; this is a real, if partial, gap in the skill's own
calibration guidance for DRY specifically.

**Same-day attempt at a fix, then a deeper correction after a real
reseed**: `principles.md`'s DRY section stated "same knowledge, not
necessarily same text" but never its inverse — that similar *text/shape*
isn't necessarily the same knowledge, which is what both misses seemed to
get wrong. Added a concrete test: "would a future change to one require
changing the other, for the same reason?" First verification (seed7,
seed8, 2 runs): both held back from a hard flag — looked like a pass.

A proper reseed (4 more: seed9-12, bringing the post-fix sample to 6)
changed the read entirely. All 6 held back from a hard flag — 6/6 on the
surface. But rereading all 6 transcripts for *why*, not just *what*: not
one of them uses the new test, or anything resembling "these are
different business rules that happen to look alike." Every single one
reasons the same way the original passing runs did before the edit
today: "real duplication, but only two instances — rule of three isn't
met, wait for a third." That's also, on rereading, what seed3 and seed5
(both logged as clean earlier today) actually said — "real but
sample-size-one," "DRY-lite... premature." The edit didn't get adopted
because the model already has a strong, well-trodden rule-of-three
instinct that's easier to reach for than the knowledge-vs-shape
distinction being asked for, fix or no fix.

**The more useful finding sat underneath the hit/miss count, not in it**:
rule-of-three reasoning gets the *current* answer right by coincidence,
because the snippet happens to show exactly 2 instances — sitting right
on the "not yet 3" side of the heuristic's own threshold. It doesn't
generalize the way the case wants it to. If a third similarly-shaped
email builder appeared (`GoodbyeEmailBuilder`, say), rule-of-three
reasoning would flip to *recommending* an abstraction — while the actual
DRY answer would still be "no," since a third coincidentally-similar,
still-unrelated business rule is exactly as much a non-duplication as
the first two. The hit/miss tally recorded today measured the surface
action (flag or not), which rule-of-three reasoning gets right at n=2 for
the wrong reason — not whether the underlying reasoning would survive the
case actually being extended. That's a sharper, harder problem than "the
wording is missing a sentence," and not one a single edit fixes.

### The rule-of-three trap, tested directly — hypothesis confirmed

The prediction above (rule-of-three reasoning would flip to recommending
abstraction at a third instance, even though a third coincidentally
similar, still-unrelated business rule is exactly as much a
non-duplication as the first two) is testable directly: add a third
class, same shape, still a different business rule.

```java
public class GoodbyeEmailBuilder {
    public String subject(String name) { return "Sorry to see you go, " + name + "."; }
}
```

Not a scored case-file addition — a quick 4-run exploratory check
(with-skill only, sonnet) of `WelcomeEmailBuilder` +
`ReminderEmailBuilder` + this third class, together.

**Confirmed, cleanly, 4/4**: every run reasoned about the code purely in
terms of instance count against a shifting threshold, never in terms of
whether the three subject lines actually encode the same knowledge (they
don't — welcome, reminder, and goodbye are three unrelated rules that
happen to share a "greeting + name" shape). Two hard-flagged it outright
("Duplication is the real issue... flag as DRY"; "textbook duplication...
flag it as 'watch for a 4th case'"). Two held back, but by moving the
goalpost rather than recognizing the pattern — one explicitly says three
one-liners is "exactly the '1-2 times, might not repeat' case" (misreading
its own count to keep applying the heuristic), the other defers to "if a
fourth email type shows up." All four, hit or miss on the surface, treat
instance count as the actual question — never the underlying "is this
really the same knowledge" question DRY is about.

This confirms the hypothesis was right, not just plausible: rule-of-three
reasoning isn't a proxy for correct DRY judgment on this case, it's a
different heuristic entirely that happens to overlap with the correct
answer only while the instance count stays below whatever threshold the
model reaches for that run. Since the case only ever shows 2 instances,
today's benchmark structurally couldn't distinguish "the skill
understands DRY's actual boundary" from "the skill counts to three" — and
now there's direct evidence it's the latter, at least for this case
shape. Worth a genuine Case C in the real benchmark suite (not just this
exploratory check) the next time `05-dry.md` gets revisited, and probably
worth rethinking what `principles.md`'s DRY section says about the "rule
of three" itself — right now it's presented as the main operational test,
with no warning that instance-count alone doesn't distinguish real
duplication from coincidence.

### A wording fix attempt, tried and failed cleanly — worth stating plainly

Same day, restructured `principles.md`'s DRY section: moved the
knowledge question ("would a change to one require the same change to
the other, for the same reason") to the front of "How to apply it," and
explicitly demoted rule of three to a timing heuristic that only applies
once duplication is already confirmed real — with an explicit warning
that instance count alone never answers the knowledge question.

Verified against the same 3-instance scenario, 4 fresh with-skill runs:
**0/4 improved. All four hard-flagged it as a DRY violation** — worse on
the surface than the pre-fix exploratory batch (2 hard-flags, 2 soft
deferrals out of 4). None of the four transcripts show any sign of
reaching for the reordered knowledge question; all four reason about
shape and instance count exactly as before ("three near-identical
classes," "no shared abstraction," "the duplication exists today, not
'might exist later'").

Read plainly, not softened: restructuring the rule didn't move the
needle at all on this failure mode. The likely reason, on reflection —
the section's own worked "Example" paragraph is about a completely
different scenario (unmet YAGNI in build tooling, and a god-class
extraction call). There's no concrete worked example anywhere in the DRY
section showing *this* trap — three structurally identical snippets that
are still coincidentally similar, not shared knowledge. Abstract rules
about "ask if the meaning is the same" competed against a strong,
concrete pattern-match instinct ("three copies of the same shape") with
nothing equally concrete on the other side.

### Third attempt: a concrete worked counter-example — also failed

Added a second `**Example**` paragraph to the DRY section, in a
deliberately different domain from the benchmark case (three
`isEligibleFor*` checks owned by different teams, not email builders),
walking through the exact reasoning: same one-line shape, three
unrelated business rules, and why counting occurrences never settles it
even as the count grows. This was the different idea worth trying — a
worked example instead of another rule.

Reverified against the same 3-instance email-builder scenario, 4 fresh
with-skill runs (skill confirmed loaded — `tool_uses: 1` on every run):
**2 hard-flagged, 2 held back** — statistically indistinguishable from
the very first exploratory batch before any edit was made (also 2/4).
One of the two soft responses used the word "unrelated" for the first
time across every transcript collected today ("three unrelated
one-liners don't yet justify an abstraction") — the closest any response
got to the actual reasoning — but then still fell back to "add one only
when a fourth builder appears," the same count-based deferral as
everything else.

Three independent fix attempts today — an added sentence, a restructured
section, and a concrete worked example in a different domain — produced
0/4, 0/4, and 2/4 (no better than baseline) respectively. Stopping here:
three clean failures on three genuinely different approaches is itself
the finding. This isn't a wording problem in `principles.md` that a
fourth attempt is likely to crack; it reads more like a pattern-match
instinct ("N similarly-shaped snippets = duplication") that resists
in-context correction regardless of how the correction is framed. Left
as a documented, unresolved limitation, not a solved one — worth
revisiting with a different mechanism entirely (e.g. a real Case C added
to the scored benchmark suite, tested across a broader set of principle
wordings) rather than another same-day patch attempt.

### SOLID and Readability — the second batch, mostly clean, one more confound found

Picked as the next two principles in line, not for any known N=2 issue —
unlike DRY/CQS/Fail Fast, nothing here was expected to be interesting.

**SOLID**: Case A clean 4/4 (all four named Dependency Inversion or
equivalent language). Case B 3/4 clean, explicit — one miss: seed3
baseline never engaged the "does this need an interface" question at
all, finding other real issues (locale bug in `String.format`, missing
validation) and staying silent on the calibration point. Same shape as
today's other Case B ambiguities, single occurrence, not investigated
further given the batch size.

**Readability**: Case A clean 4/4, strong across both models (naming,
nesting, magic numbers, the redundant comment all caught, several
responses independently proposed the same flattened boolean expression).
Case B: **4/4 never engaged the actual calibration point** — every
response (both arms, both seeds) found and focused on other real issues
instead (`LocalDate.now()` not injected via `Clock`, `datesUntil`
throwing on a past `target`, the method name/comment scoping to weekends
only vs. "business days" implying holidays too) without ever explicitly
saying "this comment earns its place, no readability issue here." Fourth
case file with this exact confound shape today (after Fail Fast Case B,
CQS Case B, and — differently — DRY's rule-of-three trap): real secondary
issues in the snippet consistently out-compete the specific thing being
tested.

**Fixed same day, partial success — worth stating precisely rather than
rounding up**: injected `Clock` (removing the testability tangent) and
added a sentence scoping the comment to explicitly exclude holidays
(removing that tangent), plus a parenthetical telling a reviewer the
`Clock` question is already answered. Reverified with 1 run per arm:

- Baseline: for the first time, explicitly said "the holiday-scope
  comment is good" — direct engagement with the actual calibration
  point, a real improvement. But then hard-flagged a *new* issue at
  blocking severity: `datesUntil` throws on a past `target`, undocumented.
- With-skill: praised the `Clock` fix by name ("the `Clock` parameter
  already gives it good testability") — confirms that specific tangent
  is closed. But raised two different new issues instead: the same
  past-`target` exception, and the exclusive-end-date semantics of
  `datesUntil`, plus a naming complaint (`daysUntil` undersells that it's
  business-days-only).

Read plainly: the fix closed the two tangents it targeted (testability,
holiday scope) — confirmed by name in both responses — but the case
still doesn't cleanly isolate its point, because different real issues
(mostly the past-date exception, not previously prominent) took their
place. This is the same whack-a-mole shape DRY's fix attempts had, on a
smaller scale: removing one distraction doesn't guarantee nothing else
is waiting to fill the gap. Not attempting a second same-day patch —
2 runs isn't enough to design a fix against, and the honest state is
"measurably improved, not clean," which is what's recorded here rather
than a premature "fixed."

### Value Object, Tell Don't Ask, Law of Demeter — seed3 only, and a pattern too big to keep patching one file at a time

Same "next in line" selection as SOLID/Readability. Results (12 runs,
seed3 only):

- **Value Object**: Case A 2/2 hit (baseline named it generically —
  "extract to EmailValidator utility" — with-skill named the principle
  directly). Case B: with-skill explicit hit ("I considered introducing a
  TrackingNumber Value Object... but... that's YAGNI"); baseline silent
  on the calibration point, found other real issues (null/blank
  validation, em-dash encoding) instead.
- **Tell Don't Ask**: Case A baseline **missed** — flagged null checks
  and "no return value," never the actual Tell Don't Ask violation
  (business decision made outside the object via getters); with-skill
  hit explicitly ("Tell Don't Ask / Anemic Domain Model — Controller asks
  Order questions and decides..."). Case B: with-skill hit explicitly
  ("that's exactly what Tell Don't Ask wants from a controller"); baseline
  silent on the point, found other real issues (IDOR risk, missing
  defensive copy on the list field) instead.
- **Law of Demeter**: Case A baseline **missed** — flagged NPE risk and a
  naming mismatch, never named the getter-chain coupling; with-skill hit
  explicitly. Case B: baseline silent on the point, but caught a genuine
  bug in the snippet itself — `priceOf` prints raw `cents` ("USD 1050"
  instead of "USD 10.50"), a real formatting defect, not a design smell.
  With-skill confirmed the same bug **and** pushed back on the
  Value-Object navigation pattern the case is designed to validate as
  fine ("Money is a primitive-obsessed record... Value Object should own
  its own `format()`... rather than letting `ReceiptPrinter` reach into
  it") — never says "Law of Demeter violation," but the substance argues
  against the exact navigation the calibration point says is normal,
  filed under a different principle's name instead.

**The pattern across today, stated plainly**: "Case B (calibration)
response finds real secondary issues and stays silent on the specific
point being tested" has now happened on **7 different principles today**
— CQS, Readability, Value Object, Tell Don't Ask, and Law of Demeter (on
top of the 2 already root-caused and fixed, Fail Fast and CQS again). At
this frequency, it stops looking like 5-7 independent case-file defects
each needing its own bespoke fix, and starts looking like a structural
property of how "should NOT flag" calibration snippets get written and
read: a snippet realistic enough to contain other genuine, interesting
issues will reliably pull a careful reviewer's attention toward those
issues and away from the one specific thing being measured — the more
realistic and well-written the snippet, the more true this gets. Fixing
each instance by hand (declare fields, add a scoping comment, inject a
`Clock`) has worked twice today (CQS, partially Readability) and is
real, worthwhile work — but doing it for every Case B in the suite isn't
a fix, it's a treadmill against the same underlying tension. Worth a
grading-methodology fix instead of more case-file patches: e.g. scoring
Case B on "did it correctly avoid or reason past the *specific* flagged
concern" as one axis, separately from "did it also find real, unrelated
issues" as a *second*, non-penalized axis — right now both get folded
into one MATCH/MISS call that's arguably measuring two different things
at once. Not implemented here; flagged as the more valuable next step
than continuing to hand-fix individual case files.

**Law of Demeter's `priceOf` formatting bug — fixed and reverified, with
one honest self-correction along the way**: replaced the raw-cents print
with `"%s %s%d.%02d".formatted(...)` — first attempt used plain `/` and
`%` on a signed `long`, which the very first verification run (baseline)
correctly caught as broken for negative `cents` (refunds): `-150` cents
produced `"-1.-50"` instead of `"-1.50"`. Fixed properly with
`Math.abs()` and a sign handled once, same day, same pass — a small
reminder that "verify the fix" applies to fixes made *inside* this
report, not just the ones inherited from earlier sessions.

Reverified with 2 more runs against the corrected version: **no run
flagged the negative-cents bug or the original raw-cents formatting
again**, and — genuinely encouraging — one with-skill run gave the
cleanest possible pass on the actual calibration point yet seen today:
"I considered flagging `item.unitPrice().currency()`-style chaining as a
Law of Demeter violation, but it's just one hop through a returned value
object, not a chain into a stranger's internals — fine as is," almost
word-for-word the case's own Expected reasoning. Both models also
independently caught a real, if extremely remote, edge case
(`Math.abs(Long.MIN_VALUE)` silently returns a negative number) —
correct and convergent, but not chased further: `Long.MIN_VALUE` cents
is astronomically unrealistic for a receipt line, the same call this
project has made on other far-edge cases today (`Integer.MAX_VALUE` day
counts, etc.).

### Value Object, Tell Don't Ask, Law of Demeter — seed4 added, now full N=4

Combined seed3+seed4: **Case A recall 8/12 clean**. All 4 baseline misses
sit on 2 principles specifically — Tell Don't Ask (2/2 baseline misses)
and Law of Demeter (2/2 baseline misses) — Value Object's baseline went
2/2 clean both seeds. With-skill: 6/6 clean across all three principles,
both seeds, no misses. Consistent with today's overall pattern: with-skill
Case A recall has been at or near 100% all day; baseline recall is the
one that varies by principle.

Case B stays the harder axis it's been all day — seed4 adds one more
partial-ambiguous response per principle on top of seed3's, no new hard
misses. One new, real finding from Law of Demeter's seed4 baseline:
it flagged that the fixed 2-decimal-place formatting in the corrected
`priceOf` (`%02d`) is wrong for currencies that don't use 2 decimals
(JPY has 0, some currencies use 3) — technically correct, and a real
gap in the fix. **Not chased further**: the case snippet never varies
`currency` across test data, doesn't claim to be currency-format-complete,
and building real per-currency decimal handling into a Law of Demeter
calibration fixture would be scope creep in exactly the direction this
report has been arguing against all day (adding realism invites more
distraction, not less). Noted here, same treatment as the `Long.MIN_VALUE`
edge case — a correct, low-priority observation, not a blocking defect.

### Strategy and Specific Exceptions — seed3 only (N=3)

Next two in line, no cause. Results (8 runs, seed3 only):

- **Strategy**: Case A baseline **missed** — worse than silent, it
  explicitly dismissed the actual issue ("Hardcoded fee values are
  fine—they're isolated business constants, not scattered magic
  numbers"), focusing instead on null handling and `BigDecimal`.
  With-skill hit cleanly ("Growing if-else chain violates Open/Closed
  Principle; use Strategy pattern"). Case B: both arms silent on
  whether Strategy is warranted for 2 stable branches — both flagged the
  same NPE risk and magic-number nit instead, neither explicitly
  reasoned through "2 branches, no growth, plain conditional is right."
- **Specific Exceptions**: Case A clean 2/2, both arms named
  `NumberFormatException` specifically. Case B: with-skill gave a clean
  explicit pass ("this is a global `@ExceptionHandler` fallback... a
  legitimate generic entry point per Fail Fast guidance, not a handler
  swallowing exceptions mid-flow"); baseline landed close to explicit
  too ("acceptable as a last-resort catch-all") before pivoting to other
  real findings (`InterruptedException` swallowing, masked security
  exceptions) — the most baseline has engaged a Case B calibration point
  directly all day, worth noting as a genuine partial counter-example to
  today's dominant pattern.

## What this does and doesn't establish

- Confirms Fail Fast's fix generalizes, mostly — the main open question
  carried over from 2026-08-19 is answered for Case A and the
  boundary-completeness point. But a same-day [blind grading
  pass](2026-08-22-blind-grading.md) caught a real miss this report's
  own same-session grading passed cleanly: a genuine factual error in
  Case B's premise (a bean-validation claim that was simply wrong),
  independently spotted by 2 different with-skill responses. Fixed the
  case file same day and reverified with 4 fresh with-skill runs: 4/4
  clean — see the blind-grading report for how the original error was
  found.
- Finds and fixes one case-file defect (`07-cqs.md` Case B) with the same
  root cause as the Fail Fast one — same day, verified.
- Investigates a CQS Case A baseline recall wobble down to real ~50%
  variance (not a systematic gap) — a working hypothesis about semantic
  vs. syntactic smells, not proven.
- Investigates a DRY Case B with-skill miss, and tries — three times — to
  fix it in `principles.md`, honestly reporting all three attempts as
  failed. The surface hit/miss tally (4/6, then 6/6 after edit #1) was
  measuring the wrong thing: nearly every run reasons via rule-of-three
  timing, not recognizing coincidental similarity. Confirmed that
  directly with a 4-run exploratory check adding a third, still-unrelated
  variant — 4/4 reasoned about instance count, none about meaning. Edit
  #2 restructured the section (knowledge question before rule of three)
  — 0/4 improved, worse than before. Edit #3 added a concrete worked
  counter-example in a different domain — 2/4, statistically the same as
  the unedited baseline. Three different approaches, three clean
  failures: this reads like a pattern-match instinct that resists
  in-context correction, not a wording gap a fourth attempt is likely to
  close. All three edits left in place (each correct on its own merits)
  but none shown to fix anything — a genuinely open, unresolved
  limitation, not a solved problem. See the report for the full,
  three-times-reversed chase.
- Extends N=4 to 2 more principles (SOLID, Readability), picked in order,
  not for cause. SOLID came back clean (7/8, one isolated Case B miss).
  Readability Case A was strong; Case B is a **fourth case file with the
  same confound shape** found today — fixed the two dominant distractions
  (untestable `LocalDate.now()`, undocumented holiday scope), reverified
  1 run per arm: both distractions are confirmed gone (named explicitly
  in both responses), but two previously-secondary issues (an
  undocumented past-date exception, exclusive-end-date semantics) became
  prominent in their place. Reported as "measurably improved, not clean"
  rather than rounded up to fixed — real progress, still an open case.
- Extends further to Value Object, Tell Don't Ask, and Law of Demeter —
  brought to full N=4 (seed3, then seed4). Case A recall: 8/12 clean —
  all 4 baseline misses concentrated on 2 principles (Tell Don't Ask,
  Law of Demeter, 2/2 misses each), with-skill 6/6 clean across all
  three. The bigger finding is about Case B, not any one principle: the
  "finds real secondary issues, stays silent on the specific calibration
  point" pattern has now shown up on **7 principles today**, not 1-2 —
  past the point where hand-fixing each case file individually is the
  right response. Flagged as a grading-methodology question (score the
  specific point and unrelated findings as two separate axes, not one
  folded MATCH/MISS) rather than case-file patching, as the more
  valuable next step.
- Fixes the `04-law-of-demeter.md` formatting bug found above, same
  session — first attempt introduced a *new* bug (broken on negative
  cents), caught by the very first verification run and fixed properly
  before moving on. Reverified clean, 2 more runs: no formatting bug
  resurfaces, and one with-skill run gives the cleanest calibration pass
  of the day, nearly word-for-word matching the case's own Expected
  reasoning.
- Extends to 2 more principles (Strategy, Specific Exceptions) at
  seed3 only (N=3). Strategy Case A: baseline missed and explicitly
  dismissed the actual issue as fine, with-skill hit clean. Specific
  Exceptions Case A: 2/2 clean. Both principles' Case B stayed mostly
  silent on the specific point, except one partial counter-example —
  baseline's Specific Exceptions Case B response landed close to an
  explicit pass before moving to other findings, the most direct
  baseline engagement with a calibration point seen all day.
- Also validates the new two-axis Case B rubric blind on 5 principles
  (Tell Don't Ask, Value Object, Readability, CQS, Law of Demeter) — see
  [the blind-grading report](2026-08-22-blind-grading.md). Finds a
  clean, arm-correlated split on 2 clean case files (with-skill
  EXPLICIT, baseline SILENT), no correlation on 2 known-confounded ones,
  and a different failure mode on the fifth (with-skill CONTRADICTED
  the point it explicitly engaged, worse than baseline's mere silence)
  — four different, internally consistent stories from one rubric.
- Does not scale N=4 to the remaining 10 untouched principles (12 minus
  the 2 just brought to N=3) — those still stand at N=2, per
  [the 2026-08-19 report](2026-08-19-principles-and-rerun.md).
